/* eslint-disable react/prop-types */
// import React from 'react';

const Cousin = ({name}) => {
    return (
        <div>
            <h4>Cousin: </h4>
            <p>{name}</p>
        </div>
    );
};

export default Cousin;